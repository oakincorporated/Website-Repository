
<!doctype html>

<html lang="en">
	<head>
		<title>Log In</title>
		<link href="styles.css" type="text/css" rel="stylesheet" />
	</head>
	<body>
		<h1>Log In</h1>
		{{#if msg}}
 			<p class="msg">{{msg}}</p>
		{{/if}}
		<fieldset>
		<form action="/login" method="post">
			<p><label>Username<br><input type="text" name="user" placeholder="Your username" maxlength="20" value="{{user}}" 
			autofocus oninput="this.value=this.value.toLowerCase()" required></label></p>
			<p><label>Password<br><input type="password" name="pass" placeholder="Your password" value="" required></label></p>
			<p><label><input type="submit" value="Log in"></label></p>
		</form>
		</fieldset>
	</body>
</html>



